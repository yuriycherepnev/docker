# Проект для запуска докер контейнеров.

### Содержит в себе DockerFiles, compose yaml files, конфиги и базы данных.

### Чтоб запустить нужный сервис, из директории проекта необходимо прописать:

```
docker compose up --build -d
```