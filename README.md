# Docker

Проект для запуска и тестирования докер сервисов.

Содержит в себе DockerFiles, compose yaml files, конфиги, базы данных, 
а также разную вспомогательную информацию.

Чтоб запустить нужный сервис, из директории проекта необходимо прописать:

```
docker compose up --build -d
```